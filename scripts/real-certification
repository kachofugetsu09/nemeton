#!/usr/bin/env bash
set -euo pipefail

repo_root=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
cd "$repo_root"

NEMETON_REAL_PROVIDER_CERTIFICATION=1 \
  go test -tags=realproviders -run '^TestRealProviderCertification$' -count=1 -timeout=20m -v ./internal/meeting
